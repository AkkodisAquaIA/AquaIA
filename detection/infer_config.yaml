# AquaIA - DETR+DINO inference config

run:
  runs_root: "results/detect/yolo26_s_random"
  run_dir: null

data:
  test_data_root: "datasets/coco8"

inference:
  batch: 2
  imgsz : 640
  conf: 0.3
  num_samples: 20
  seed: 0

output:
  output_dir: null
