# AquaIA - DETR+DINO inference config

run:
  runs_root: "results/detect/yolo26_s_random"
  run_dir: null

data:
  test_data_root: "datasets/coco_custom"
  loader: "jpg"
  split: "test"

inference:
  batch: 8
  imgsz : 640
  conf: 0.3
  num_samples: 20
  seed: 0

output:
  output_dir: null
