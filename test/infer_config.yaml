# AquaIA - DETR+DINO inference config

run:
  runs_root: "results/detection/detr"
  run_dir: null

data:
  test_data_root: "datasets/coco1k"

inference:
  batch: 8
  conf: 0.3
  num_samples: 20
  seed: 0

output:
  output_dir: null
